<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >

    <!-- phone+portrait version included in layout/ dir -->
    <pt.thingpink.views.TPFontableButton
        android:id="@+id/bt_card_form_submit_button"
        style="@style/bt_submit_button"
        android:layout_alignParentBottom="true"
        android:textColor="@drawable/white_to_black_text_selector"
        app:font="Gotham-Medium.otf"
        android:background="@drawable/select_button_background"
        android:layout_marginTop="@dimen/bt_form_padding" />

</merge>

